
from app import db

class PortalText(db.Model):
    __tablename__ = 'portals_texts'

    id = db.Column(db.Integer, primary_key=True)
    portals_id = db.Column(db.Integer, db.ForeignKey('portals.id'), nullable=False)
    title = db.Column(db.String(255), nullable=True)
    text = db.Column(db.Text, nullable=True)

    portal = db.relationship('Portal', backref='portal_texts')

    def __repr__(self):
        return f"<PortalText id={self.id} portal_id={self.portals_id} title=