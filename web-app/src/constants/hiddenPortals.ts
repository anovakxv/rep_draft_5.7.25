// Portals hidden from the public "ALL" tab browse/search list on MainScreen (mobile)
// and DesktopDashboard (desktop). They remain fully accessible everywhere else:
// via direct link, the Network tab, and the portal page itself.
//
// Portal 93 (Capital HS reunion) is distributed via its direct link only, so it is
// kept off the public discovery list.
export const HIDDEN_ALL_TAB_PORTAL_IDS: number[] = [93];
